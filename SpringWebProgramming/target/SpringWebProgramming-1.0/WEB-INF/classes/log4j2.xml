<?xml version="1.0" encoding="UTF-8"?>

<Configuration status="WARN">
	<!-- 어디에 Log를 출력할 것인지 설정 -->
	<Appenders>
		<Console name="Console" target="SYSTEM_OUT">
			<PatternLayout pattern="%d{HH:mm:ss} %-5level %logger{2} %M() - %msg%n" />
		</Console>
	</Appenders>

	<!-- 로깅 대상(클래스) -->
	<!-- 로그를 출력하는 클래스를 Logger라고 부름 -->
	<Loggers>
		<Root level="info">
			<AppenderRef ref="Console" />
		</Root>
	</Loggers>
</Configuration>